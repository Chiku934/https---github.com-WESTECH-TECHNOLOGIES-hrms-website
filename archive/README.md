# Archived HTML

The original `.html` pages were moved here during the PHP migration.

They are kept only as a fallback reference while the new PHP pages in `public/` and `app/` are used by the live application.

Do not edit these files unless you intentionally need to compare against the pre-migration markup.
